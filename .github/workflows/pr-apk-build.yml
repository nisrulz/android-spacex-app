name: Test Label Event

on:
  pull_request:
    types: [labeled]

jobs:
  test-label-event:
    runs-on: ubuntu-latest
    steps:
      - name: Check Label Event
        run: echo "Label event triggered: ${{ github.event.action }}"
